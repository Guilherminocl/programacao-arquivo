#include <stdio.h>

int main() {
    int vezes, media, d, n;
    printf("Quantas vezes deseja repetir?\n");
    scanf("%d", &vezes);

    for (int i = 0; i < vezes; i++) {
        scanf("%d", &d);
        int matriz[n] = d;
        media = n/vezes
        printf("Olá, mundo!\n");
    }
    return 0;
}